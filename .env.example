# LOCAL DEVELOPMENT ONLY. These are public example values, not production secrets.
# Copy to .env at the repository root; never commit .env.
POSTGRES_DB=rook_local
POSTGRES_USER=rook_local
POSTGRES_PASSWORD=rook_local_development_only
